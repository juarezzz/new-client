import { Typography } from "@mui/material";
import { Box } from "@mui/system";

function Create() {
    return (
        <Box>
            <Typography textAlign='center' variant='h4'>Add a book</Typography>
        </Box>
    );
}

export default Create;
